<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Dewsverse Video Conference
    </title>
    <script>
        const ROOM_ID = "<%= roomId %>";
    </script>
    <script src="https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <script src="function.js" defer></script>
    <script src="https://cdn.tailwindcss.com">
    </script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <style>
        #video-grid {
            place-self: center;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 4px;
            padding: 4px;
        }

        video {
            width: 100;
            height: 100;
            object-fit: cover;
            border: 2px solid #ffff2e;
            border-radius: 13px;
            place-self: center;
        }

        video::after {
            content: "Status: Conntected";
            display: block;
            z-index: 100;
        }
    </style>
</head>


<body class="bg-gray-800">
    <div class="flex flex-col h-screen">
        <!-- Video Grid -->
        <h3 class="text-center text-white text-[21px] mt-[11px] mb-[5px]">Anas's Room</h3>
        <div id="video-grid" class="w-[80%] grid grid-cols-1 md:grid-cols-2 gap-4 p-4 justify-self-center"></div>
        <!-- Controls and Chat -->
        <div class="flex flex-1 items-end p-4" style="align-self: center;">
            <!-- Controls -->
            <div style="background: #3b4053;padding: 13px;border-radius: 18px;"
                class="flex justify-between items-center">
                <div style="" class="flex space-x-2 mr-3">
                    <button class="bg-gray-700 text-white p-2 rounded-md focus:outline-none">
                        <i class="fas fa-microphone">
                        </i>
                        Mute
                    </button>
                    <button class="bg-gray-700 text-white p-2 rounded-md focus:outline-none">
                        <i class="fas fa-video">
                        </i>
                        Stop Video
                    </button>
                    <button onclick="leaveMeeting();" class="bg-red-700 text-white p-2 rounded-md focus:outline-none">
                        <i class="fas fa-video">
                        </i>
                        Leave
                    </button>
                </div>
                <div style="" class="hidden md:block flex space-x-2">
                    <button onclick="inviteOthers()" class="bg-[#ffff2e] text-black p-2 rounded-md focus:outline-none">
                        <i class="fas fa-user-friends">
                        </i>
                        Invite
                    </button>
                    <button class="bg-gray-700 text-white p-2 rounded-md focus:outline-none">
                        <i class="fas fa-comment-alt">
                        </i>
                        Chat
                    </button>
                    <button class="bg-gray-700 text-white p-2 rounded-md focus:outline-none">
                        <i class="fas fa-desktop">
                        </i>
                        Screen Share
                    </button>
                </div>
            </div>
        </div>
</body>

</html>